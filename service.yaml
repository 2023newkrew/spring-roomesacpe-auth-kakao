apiVersion: v1
kind: Service
metadata:
  labels:
    app: soony-was
  name: soony-was-svc
spec:
  ports:
    - name: 8080-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: soony-was
  type: ClusterIP
